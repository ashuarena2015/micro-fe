import React from 'react';
import { Flex } from 'antd';

const Profile = () => {
    return (
        <Flex style={{ height: '100vh' }}>
            <Flex>
                Left
            </Flex>
            <Flex>
                Right
            </Flex>
        </Flex>
    )
}

export default Profile;
